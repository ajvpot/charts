#
# IMPORTANT NOTE
#
# This chart inherits from our common library chart. You can check the default values/options here:
# https://github.com/k8s-at-home/library-charts/tree/main/charts/stable/common/values.yaml
#

controller:
  # -- Number of pods to load balance between
  replicas: 1

image:
  # -- image repository
  repository: ghcr.io/ajvpot/vanderbot/vanderbot
  # -- image tag
  tag: latest@sha256:047f01732d2ced7091492c22273a572120937a8c81d95a5a2293a90f26ad2c36
  # -- image pull policy
  pullPolicy: Always

# -- environment variables. See more environment variables in the [nullserv documentation](https://github.com/bmrzycki/nullserv/blob/master/README.md).
# @default -- See below
env:
  # -- Set the container timezone
  TZ: UTC

ingress:
  # -- Enable and configure ingress settings for the chart under this key.
  # @default -- See values.yaml
  main:
    enabled: false

# -- Configures the probes for the main Pod.
# @default -- See values.yaml
probes:
  readiness:
    #enabled: true
    #custom: true
    spec:
      httpGet:
        path: /welcome.txt
        port: http
